<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      NGUYÊN TẮC MINH BẠCH (xem docs/01-de-tai.md)
      - Ứng dụng LUÔN hiển thị icon + tên trong launcher (không có alias ẩn).
      - Mọi quyền dưới đây chỉ được yêu cầu SAU khi người dùng đọc màn hình
        Onboarding giải thích rõ mục đích, và đều có thể từ chối từng phần.
      - Ứng dụng chỉ đọc dữ liệu của CHÍNH thiết bị đang cài và hiển thị tại chỗ.
        Không có thành phần mạng, không có receiver nhận lệnh điều khiển từ xa.
    -->

    <!-- (1) Thu thập: thống kê sử dụng ứng dụng (quyền đặc biệt, cấp qua Settings) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- (1) Thu thập: liệt kê ứng dụng đã cài + quyền của chúng -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- (1) Thu thập: trạng thái mạng để thống kê kết nối -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- (1)(2) Media: đọc để lập chỉ mục và khôi phục từ thùng rác MediaStore -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- (2) Phục hồi: quét sâu vùng lưu trữ dùng chung. Tùy chọn, người dùng
         phải tự bật trong Settings; ứng dụng chạy đầy đủ nếu không cấp. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- (1) Tùy chọn: chỉ dùng cho module "Dữ liệu cá nhân trên máy" -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Hiển thị kết quả thu thập nền -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".DeviceGuardApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeviceGuard">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DeviceGuard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- (1) Nhật ký thông báo. Người dùng phải bật thủ công trong
             Settings > Notification access; Android hiển thị cảnh báo rõ ràng. -->
        <service
            android:name=".data.collector.NotificationLogService"
            android:exported="false"
            android:label="@string/notification_service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
